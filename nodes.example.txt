# One proxy URL per line (project-owned egress; no Clash required)
# Copy to nodes.txt or merge into nodes.json via:
#   python -m register_core nodes add 'http://user:pass@host:port'
#
# http://user:pass@1.2.3.4:8080
# http://user:pass@5.6.7.8:8080
